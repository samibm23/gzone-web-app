<form method="post" action="{{ path('app_user_game_preferences_delete', {'id': user_game_preference.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_game_preference.id) }}">
    <button class="btn">Delete</button>
</form>
