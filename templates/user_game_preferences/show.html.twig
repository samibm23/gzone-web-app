{% extends 'base.html.twig' %}

{% block title %}UserGamePreferences{% endblock %}

{% block body %}
    <h1>UserGamePreferences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_game_preference.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_game_preferences_index') }}">back to list</a>

    <a href="{{ path('app_user_game_preferences_edit', {'id': user_game_preference.id}) }}">edit</a>

    {{ include('user_game_preferences/_delete_form.html.twig') }}
{% endblock %}
