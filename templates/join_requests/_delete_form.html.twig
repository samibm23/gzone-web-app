<form method="post" action="{{ path('app_join_requests_delete', {'id': join_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ join_request.id) }}">
    <button class="btn">Delete</button>
</form>
