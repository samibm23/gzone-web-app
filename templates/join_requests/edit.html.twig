{% extends 'base.html.twig' %}

{% block title %}Edit JoinRequests{% endblock %}

{% block body %}
    <h1>Edit JoinRequests</h1>

    {{ include('join_requests/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_join_requests_index') }}">back to list</a>

    {{ include('join_requests/_delete_form.html.twig') }}
{% endblock %}
