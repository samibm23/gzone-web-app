{% extends 'admin.html.twig' %}

{% block title %}StoreReports{% endblock %}

{% block body %}
    <h1>StoreReports</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ store_report.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_store_reports_index') }}">back to list</a>

    <a href="{{ path('app_store_reports_edit', {'id': store_report.id}) }}">edit</a>

    {{ include('store_reports/_delete_form.html.twig') }}
{% endblock %}
