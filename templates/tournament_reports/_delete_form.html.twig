<form method="post" action="{{ path('app_tournament_reports_delete', {'id': tournament_report.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournament_report.id) }}">
    <button class="btn">Delete</button>
</form>
