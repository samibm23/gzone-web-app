{% extends 'base.html.twig' %}

{% block title %}Edit Posts{% endblock %}

{% block body %}
<div class="nk-widget nk-widget-highlighted">
    <h1 class="nk-widget-title"><span><span class="text-main-1">Edit</span> Post</span></h1>
    <div class="nk-widget-content">
   

    {{ include('posts/_form.html.twig', {'button_label': 'Update'}) }}

       <blockquote class="nk-blockquote"></blockquote>


    {{ include('posts/_delete_form.html.twig') }}
    <blockquote class="nk-blockquote"></blockquote>
     <a href="{{ path('app_posts_index') }}" class="nk-btn nk-btn-rounded nk-btn-color-main-1">back to list</a>
{% endblock %}
