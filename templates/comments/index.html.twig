{% extends 'base.html.twig' %}

{% block title %}Comments index{% endblock %}

{% block body %}
    <h1>Comments index</h1>

    <table class="nk-table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CommentBody</th>
                <th>CommentDate</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for comment in comments %}
            <tr>
                <td>{{ comment.id }}</td>
                <td>{{ comment.commentBody }}</td>
                <td>{{ comment.commentDate ? comment.commentDate|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_comments_show', {'id': comment.id}) }}" class="nk-btn nk-btn-rounded nk-btn-color-main-1">show</a>
                    <a href="{{ path('app_comments_edit', {'id': comment.id}) }}" class="nk-btn nk-btn-color-primary">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <blockquote class="nk-blockquote"></blockquote>

    <a href="{{ path('app_comments_new') }}" class="nk-btn nk-btn-rounded nk-btn-color-main-1">Create new</a>
{% endblock %}
