{% extends 'base.html.twig' %}

{% block title %}Edit Comments{% endblock %}

{% block body %}
<div class="nk-widget nk-widget-highlighted">
    <h1 class="nk-widget-title"><span><span class="text-main-1">Edit</span> Comment</span></h1>
    <div class="nk-widget-content">
    
<div class="nk-gap-1"></div>
    {{ include('comments/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="nk-gap-1"></div>

    {{ include('comments/_delete_form.html.twig') }}
    <div class="nk-gap-1"></div>
    <a href="{{ path('app_posts_show', {'id': postId}) }}" class="nk-btn nk-btn-rounded nk-btn-color-main-1">back to post</a>
{% endblock %}
